# ar-autocommit

Terminal utility to automatically create a commit with an AI-generated summary of changes.

## Building

Before building the project, run `set_env.sh` to set up the necessary environment variables for the build process. If
you omit this step, you will find that the runtime fails with a 'builder error'.

## Dependencies

* `libgit2` (`brew install libgit2`)